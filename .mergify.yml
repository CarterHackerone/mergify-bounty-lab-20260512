queue_rules:
  - name: default
    queue_conditions:
      - check-success=lab-ci
    merge_conditions:
      - check-success=lab-ci
    merge_method: squash

pull_request_rules:
  - name: head config label action probe
    conditions: []
    actions:
      label:
        add:
          - head-config-executed-20260512
