queue_rules:
  - name: selfmerge-probe
    queue_conditions: []
    merge_conditions: []
    merge_method: squash

pull_request_rules:
  - name: self merge probe from PR head config
    conditions:
      - label=self-merge-probe-20260512
    actions:
      queue:
        name: selfmerge-probe
