extends: mergify-private-lab-20260512

pull_request_rules:
  - name: public PR references private queue marker
    conditions:
      - label=merge-queue
    actions:
      queue:
        name: private-extends-marker-does-not-exist-20260512
